# graph_models




```
# Graph picture of all (-a) apps; (-g) means group tables by app
python manage.py graph_models -a -g -o db-visual.png

# Graph picture of specified apps
python manage.py graph_models -g -o db-visual-specified_apps.png assessments clients sales

```
